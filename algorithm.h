#pragma once
#include <vector>
#include "Graph.h"

class algorithm
{
private:
	Graph<Node> g;
	std::vector<int>* previousNode;
	std::vector<double>* wayValue;

public:
	//algorithm();
	//~algorithm();
};

